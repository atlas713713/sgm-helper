# 功能说明

本文档记录“三国 M 助手”当前已经实现的功能、入口、默认行为和边界。安装与构建方式见 [README.md](README.md)。

状态说明：

- **已实现**：当前代码包含完整入口和执行流程。
- **部分实现**：可以使用，但仍有明确未覆盖的步骤。
- **未实现**：界面中保留提示或规划，当前不能执行。

## 功能总览

| 模块 | 功能 | 状态 | 主要入口 |
| --- | --- | --- | --- |
| 基础 | 悬浮菜单、进度提示、设置持久化 | 已实现 | 助手无障碍服务 |
| 启动 | 公告、开始游戏、快捷登录、账号登录、福利遮挡处理 | 已实现 | 启动游戏时自动执行 |
| 练级 | 荒野选区、选怪、标记点练级 | 已实现 | 辅 → 练级 |
| 练级 | 野境独立导航 | 部分实现 | 当前复用标记点流程 |
| BOSS | 野王寻敌、攻击、循环换线 | 已实现 | 辅 → BOSS → 野王 |
| BOSS | 跟随队长分流 | 已实现 | 设置 → BOSS |
| BOSS | 世界王 | 未实现 | 界面仅显示待实现 |
| 副本 | 多等级顺序扫荡、定时扫荡 | 已实现 | 辅 → 副本 / 定时任务 |
| 军务 | 物资、荒野任务承接与完成 | 已实现 | 练级前检查 / 定时任务 |
| 军务 | 勇讨军团天将 | 未实现 | 自动军务会跳过 |
| 日常 | 膜拜、军团奖励、福利领取 | 已实现 | 定时任务 / 立即执行 |
| 日常 | 天降 BOSS | 已实现 | 定时任务 / 立即执行 |
| 辅助 | 复活士兵 | 已实现 | 辅 → 复活士兵 |
| 辅助 | 背包检测与自动出售 | 已实现 | 设置 → 练级 |
| 辅助 | 定时重启游戏（主线空闲时执行） | 已实现 | 设置 → 辅助 |
| 诊断 | 分模拟器日志、最新日志、故障截图、有限恢复 | 已实现 | 自动运行 |

## 悬浮界面与任务模型

无障碍服务启用后，游戏左下角显示可拖动的“辅”按钮。主菜单包含：

- 练级
- BOSS
- 副本
- 复活士兵
- 终止
- 设置
- 退出

设置页分为“练级 / BOSS / 副本 / 定时任务 / 辅助”。执行任务时，顶部进度条显示当前阶段和错误信息，但不拦截游戏触摸。

助手把任务分为两类：

- **主线任务**：练级、BOSS、副本。助手会记住当前主线。
- **临时任务**：军务、福利、奖励、自动出售、复活士兵等。执行结束后恢复原主线。

“终止”会取消当前动作链，并把默认主线设回练级。

## 游戏启动与登录

打开游戏或定时任务唤起游戏时，助手会等待画面加载，并依次处理可能出现的：

- 最新公告
- 开始游戏
- 快捷登录
- 账号和密码登录
- 福利页、奖励提示及常见遮挡窗口

账号和密码保存在当前模拟器的 Android 应用私有数据中，不会在三台模拟器之间自动同步。

## 自动练级

### 荒野练级

荒野模式支持选择 `1–24` 区和目标怪物。启动后会：

1. 关闭自动攻击和遮挡窗口。
2. 进入军团荒野营地。
3. 查找荒野传送官并选择目标区域。
4. 确认地图已经加载到“荒野修炼 X 区”。
5. 选择目标怪物并开启自动攻击。

荒野怪物配置：

怪物名单和刷新区间取自武当 `arrays.xml` 的 `wild` 表。每次导航按武当 `GameUtil.o` 的做法，在怪物的 X 区间里随机取点，Y 固定用 `30`；海妖有两段刷新区，会先随机挑一段。

| 区域 | 可选怪物（等级 名字，刷新 X 区间） |
| --- | --- |
| 1–3 区 | `30 蛇魔女 30–90`、`40 火妖 110–180`、`50 土霸王 210–240`、`60 食人花 370–400`、`70 金甲龙 440–500`、`75 圣武士 520–570` |
| 4–6 区 | `80 魔斗士 30–165`、`90 海妖 200–230 / 370–415`、`100 螳蝎妖 460–570` |
| 7–9 区 | `115 九尾狐 30–230`、`130 石狮精 370–570` |
| 10–12 区 | `145 人面鸟 30–230`、`160 战鬼 370–570` |
| 13–15 区 | `175 式神童 30–230`、`190 剑齿虎 370–570` |
| 16–18 区 | `205 犬神 30–230`、`220 木人兵 370–570` |
| 19–21 区 | `235 曼陀猪 30–230`、`250 黑暗鬼 370–570` |
| 22–24 区 | `265 史前猿 30–230`、`280 死魂刀兵 370–570` |

旧版本把 `100 螳蝎妖` 写成了“螳螂巨妖”、`175 式神童` 写成了“式神童子”，`1–3` 区也漏了 `30/40/50` 三种怪。旧设置里存的错名仍然认得，不用重新选。

传送官或选区 OCR 连续失败时，助手会保存截图和识别到的 OCR 文本，再通过有限次数的主线恢复重新尝试。

### 标记点与野境

标记点模式会关闭自动攻击、使用第一个标记卷、等待地图加载，然后重新开启自动攻击。

落地后会 OCR 一次地图名。识别到「铁门峡二层」时不直接开自动攻击，而是打开自动寻路栏、切到「敌人」页签、在列表里点「黄巾刀车」，选中后再开自动攻击。其它地图不受影响。

匹配只看「刀车」两个字：同一列表里的黄巾枪骑兵、黄巾力士都不含它，而「巾/金」同音又形近，OCR 读成哪个都能命中。`0.1.348`–`0.1.349` 写的是不存在的「黄**金**刀车」，所以一直选不中。

引路列表用的是副本那套 Paddle 识别（`recognizeDungeonText` + 以「全部怪物」表头为锚点拆敌人行），不是通用全屏 OCR——通用 OCR 读不出面板里的小字，`0.1.348` 就是因此一直选不中刀车。每秒读一次、最多 10 次；仍然找不到（例如还没刷新）就把当次读到的行写进日志，关掉寻路栏、按普通标记点练级继续，不会让整条主线失败。

当前“野境”尚没有独立的导航实现，实际使用与标记点相同的启动流程。这是功能边界，不应理解为已经支持野境选区或选怪。

### 自动开始

“模拟器重启后自动开始练级”开启时，配合项目重启脚本可在实例恢复后拉起助手、游戏和练级流程。

## BOSS

### 野王

野王模式会：

1. 使用第一个标记卷并设置游戏自动模式。
2. 沿小地图横向范围移动。
3. 在移动过程中识别红色中文 BOSS 名称。
4. 发现目标后攻击，目标消失后继续搜索。
5. 完成一轮后依次切换 `1–8` 分流并继续寻找。

换线后如果出现头像方向验证，`AntiCheatVerification` 会使用人脸方向检测旋转头像并提交；验证窗口消失后才继续。

主菜单的“换线测试”会循环切换1–8分流；发现反外挂验证时不会自动解题，而是立即保存触发OCR的同一帧完整PNG并停止测试。

开启“打 BOSS 时跟随队长分流”后，野王流程会先打开队伍栏，读取右下角当前分流，再从“管理”的第一行读取队长分流。分流不一致时会切换到队长所在分流并检查反外挂验证，然后才开始寻王。寻王和战斗期间每 `10s` 检查第一格队长头像；头像变暗时重新读取并跟随队长分流。队伍栏、当前分流或队长分流连续 OCR 失败时会明确报错、记录 OCR 行并保存截图。

头像变暗属于画面亮度变化，不使用 OCR；当前阈值按照固定 `1280 × 720` 队伍栏截图校准。

### 世界王

世界王当前未实现，BOSS 页面只保留提示。

## 副本扫荡

可选等级：

```text
10、20、30、40、50、60、65、70、75、80、90、105、120、135、150、165、180
```

勾选结果会自动保存。手动或定时启动后，助手按等级从低到高逐个扫荡。定时副本如果遇到其他任务占用，会在 `60s` 后重试；完成后恢复原主线。

定时任务里的「自动副本」会先跑勾选的一般副本，再跑勾选的扫荡副本：只勾一边就只跑那一边，两边都没勾就整个任务跳过（只记日志，不提示错误）。两段都作为插入任务排队执行，中间一段失败也不影响后面一段，全部跑完再恢复原主线。精英副本不参与定时任务，只能手动启动。

## 自动军务

军务既可以在练级启动前检查，也可以由定时任务唤起。BOSS 和副本主线不会主动插入军务。

支持的任务：

| 军务 | 承接 | 执行 | 完成检测 |
| --- | --- | --- | --- |
| 补充军团物资 | 是 | 是 | OCR 读取物资数量，必要时使用完成标记兜底 |
| 巡狩军团荒野 | 是 | 是 | OCR 读取收集数量并确认当前荒野地图 |

| 勇讨军团天将 | 否 | 否 | 自动军务会跳过，见下 |

武当把「勇讨军团天将」拆成两半：`LegionTaskAction` 只负责承接（天将军官）和勾选任务行的复选框，真正打天将是另一个按军团开战时间跑的 `WarLegionAction`（去荒野 → 换到指定分流 → 打开天将面板 → 找目标 → 打 → 结束）。后半段需要军团的开战时间、分流号和持续时间，这三项只能由使用者提供，所以两半都还没实现。

核心行为：

- 每轮先检查“任务 → 进行中”，已有收集任务时不会重复回城承接。
- 定时任务页可分别勾选“军团物资”和“军团荒野”；未勾选的任务不会检查、承接或安排闹钟。两项都未勾选时，自动军务不会启动。
- 没有进行中任务时，回城进入“军团 → 可承接”，逐项判断冷却并承接。
- “快速抵达”、任务标题、冷却文本和数量支持相邻 OCR 片段合并匹配。
- 巡狩荒野每 `5min` 复查一次；军团物资每 `3min` 复查一次。
- 巡狩军团荒野括号里的数字是任务的**阶**，不是区号。按武当 `w0.c()` 换算成区段和目标怪等级后，先到区、再走到那种怪的刷新段，最后才点任务：

  | 阶 | 荒野修炼区 | 目标怪 |
  | --- | --- | --- |
  | 一/二/三/四/五/六 | 1–3 区 | `30 蛇魔女` / `40 火妖` / `50 土霸王` / `60 食人花` / `70 金甲龙` / `75 圣武士` |
  | 七 / 八 | 4–6 区 | `80 魔斗士` / `100 螳蝎妖` |
  | 九 / 十 | 7–9 区 | `115 九尾狐` / `130 石狮精` |
  | 十一 / 十二 | 10–12 区 | `145 人面鸟` / `160 战鬼` |
  | 十三 / 十四 | 13–15 区 | `175 式神童` / `190 剑齿虎` |
  | 十五 / 十六 | 16–18 区 | `205 犬神` / `220 木人兵` |
  | 十七 / 十八 | 19–21 区 | `235 曼陀猪` / `250 黑暗鬼` |
  | 十九 | 22–24 区 | `265 史前猿` |

  练级设置里的荒野区如果已经落在区段内就沿用，否则在区段里随机挑一个——和武当一致。
- 军务对话只点击左侧 NPC 窗口底部的“离开”位置，不在全屏搜索并点击“离开”。
- 补充军团物资最后一次 OCR 失败时，会保存当时截图，并把右侧 OCR 文本和坐标写入日志。
- 任务完成后重新读取冷却时间，并安排下一次检查；无法读取时使用 `3h` 兜底。
- 任一任务的独立检查时间已经过期时，会在最多 `60s` 内重新触发，不会被另一任务的未来冷却时间覆盖。
- 自动军务结束后回城，并恢复之前的练级主线。
- 任务详情里写着别的城市时，先关掉任务窗、按下面的「城市传送」过去，再重新打开任务窗承接（武当也是先到城再承接）。同一个任务只会为此跑一趟，到不了的城市直接跳过。

## 城市传送

复刻武当的 `TeleportCityAction` 和 `LegionTaskAction.npcTeleport`，坐标取武当针对本服（包名 `hk.phx.khm.cs`，`gameServerType = 2`）的那一套。

能直接传送的城市走客栈/商店：

1. OCR 当前城市；不在城里就先回城。
2. 读自己的城内坐标，人在城东（`x > 250`）走商店，城西走客栈；入口是城内地图 `(354, 3)`，三大都城（洛阳、成都、建业）是 `(361, 3)`，客栈对应 `107` / `137`。
3. 先按同一个 x、随机 y 靠过去，x 差值进入 `±5` 后再走到门口。
4. OCR 面板标题确认进了商店或客栈；商店要先点「传送」页签。没进去就退到 `y = 17` 再走一次，最多 3 轮。
5. 按城市所在的分页拖两下地图，点两下城市，再点「传送」。
6. OCR 到「确定」才点确认，然后每秒 OCR 地图名，最多等 30 次；不行就整段重来，最多 2 轮。
7. 汉中是特例：必须先传成都再传汉中。

不能直接传送的城市（建安、九原、大漠、白沙瓦）先传到中转城，再找城里的传送官：

| 目标 | 中转城 | 传送官坐标 |
| --- | --- | --- |
| 建安 | 会稽 | `(364, 18)` |
| 九原 | 凉州 或 晋阳 | `(360, 20)` |
| 大漠 | 凉州 | `(370, 12)` |
| 白沙瓦 | 凉州 | `(376, 12)` |

传送官这一跳：走到坐标（容差 `±5`，走不到就重发寻路，最多 45 次）→ 点 NPC 快捷键 → OCR 对话框第三行确认是「是」→ 点它 → 每秒 OCR 地图名等落地，20 次一轮、最多 5 轮，中间会重新点一次 NPC 和「是」。

尚未支持：合浦（传送官只送到云南，之后还要跑 云南西境 → 交趾 → 合浦）、渔阳、黄巾祭坛一层、郿坞宫殿、武都大寨（武当是传到邻城后跑地图链过去，我们还没有跑地图链的能力）。

「辅助 → 前往城市」可以手动选一座城市执行这套流程，用来验证路线。

## 自动出售

自动出售可用于练级、BOSS 和副本主线，默认关闭。开启后：

- 每 `60s` 检查一次背包容量。
- 连续读取两次容量，减少一次 OCR 抖动造成的误判。
- 剩余格数低于设置阈值时回城。
- 副本中只记录待出售事件，离开副本并领取奖励后在副本营地找“行脚商队”出售，不会中途退出副本或绕回主城。
- 清理前打开“元宝回收”，依次快速贩卖金元宝和银元宝装备。
- 副本清理按武当坐标打开营地商人的交易页；普通背包事件仍自动前往客栈，等待女老板对话，出售杂物并快速出售装备。
- 完成后恢复原主线。

阈值可设置为 `1–20` 个剩余格，默认 `5`。

## 复活士兵

“复活士兵”会打开右侧自动寻路，通过 OCR 查找“军营”。没有找到时最多滚动多次，并可回城重新查找。进入军营后等待“士兵卸下”，打开复活页面，点击“复活全部”并确认。

嵌入其他流程时，如果当前没有可复活士兵或页面无法打开，会记录并跳过，不把整个主线视为致命失败。

「设置 → 辅助 → 开始练级前自动复活士兵」默认开启。从菜单点「练级」时的前置顺序固定是**复活 → 军务 → 练级**：勾了就一定先复活，军务开着也不会被跳过；军务作为插入任务跑完后由任务管理器恢复练级主线。这个选项只作用于手动开始练级，插入任务（军务、福利、副本等）结束后恢复主线时不会重复复活。

## 定时重启游戏

长时间挂机后游戏进程占用的内存会一直涨，拖累模拟器。「设置 → 辅助 → 定时重启游戏」按固定间隔把游戏杀掉重开，间隔可选 `20/30/40/50/60/90/120/180/240/360` 分钟，默认 `120` 分钟，默认关闭。

计时和执行分开：

- 只在主线任务运行时计时；终止任务或助手停止后清零，重新开始跑时重新计时。
- 到点后**不立刻执行**，而是每分钟检查一次，等到主线任务空闲才动手。空闲的判定是：当前跑的就是主线任务本身、没有插入任务在排队、也没有背包检测或装备处理挂着。
- 因此定时军务、副本、福利这类插入任务不会被从中间打断，只会推迟这一次重启。

执行时通过 root 用 `am force-stop` 杀掉游戏进程（先试 `su -c`，再试 `su 0`），等待 5 秒后结束这次插入任务。任务管理器随后自动重跑主线任务，而主线任务本来就从“打开游戏 → 登录”开始，游戏就此重新拉起并登录。

没有 root 时命令会失败，助手记录错误并照常恢复主线任务，不会卡住。「立即重启游戏」按钮可以不等间隔直接验证这条链路；主线任务当时正忙的话，会改为在下一个空闲窗口执行。

## 福利与奖励

### 福利领取

支持以下福利类目：

- 群英商店
- 在线奖励
- 每日签到
- 每日挑战
- 每周挑战
- 群英荣耀
- 每日应援

助手会扫描红点、处理分页和滚动，并针对在线奖励、每日挑战和每日应援使用各自的领取规则。关闭福利窗口时，如果出现“尚未领取的奖励”，会选择关闭界面并继续，不让弹窗阻塞后续任务。

### 膜拜与军团奖励

- **膜拜**：进入排行榜荣耀厅，分别处理大丰收城和国战榜膜拜。
- **军团奖励**：进入军团主页领取俸禄，并执行一般贡献。

这些流程主要依赖固定坐标，因此游戏 UI 布局变化后需要重新验证。

### 天降 BOSS

天降任务会进入设置的荒野区域，在设定持续时间内：

1. 前往地图中心。
2. 周期性扫描红名 BOSS。
3. 发现目标后攻击，目标消失后回到中心继续扫描。
4. 到达截止时间后回城并恢复原主线。

持续时间可设为 `1–60min`，默认 `5min`；区域可设为 `1–15`，默认 `1` 区。

## 定时任务

所有定时任务默认启用，但只有无障碍服务保持开启时才能执行。“立即执行”不受复选框影响，可用于手动验证流程。

| 任务 | 默认时间 | 说明 |
| --- | --- | --- |
| 军团奖励 | `10:10` | 每日一次 |
| 膜拜 | `10:30` | 每日一次 |
| 领取福利 | `12:05`、`18:05` | 第二次固定在第一次后 `6h` |
| 自动副本 | `14:00` | 先一般副本后扫荡副本，各自使用已保存的等级；都没勾选就跳过 |
| 天降 | `22:00` | 默认持续 `5min`，默认荒野 1 区 |
| 自动军务 | 动态 | 军团物资和军团荒野可分别勾选；只使用已勾选任务的冷却时间安排下一次触发 |

取消普通定时任务的勾选会取消它的定时闹钟，但仍可以点击“立即执行”。自动军务还会检查它下面的两个任务选项：未勾选的军团任务即使手动触发也不会执行。修改时间后会立即保存并重新安排下一次触发。

## 错误恢复与诊断

### 恢复原则

- 自动化发生错误时先记录错误并保存故障画面。
- 前几次失败会短暂等待后恢复原主线。
- 连续失败会改用更长等待，再做最后一次有上限的恢复。
- 临时任务不会把主线永久改成登录、军务或福利，也不应无限退出和重开游戏。

### 日志

共享日志目录：

```text
/Users/Shared/Library/Application Support/BlueStacks/Engine/UserData/SharedFolder/SGMHelperLogs
```

每台模拟器按名称和稳定的 `ANDROID_ID` 写入独立文件。主日志最大约 `2 MB`，并保留一个 `.log.1` 备份。`*-latest.log` 只保留最近 `300` 行，按最新在前排列，适合直接打开查看当前状态。

日志覆盖：

- 应用和无障碍服务生命周期
- 主线切换、定时触发和任务进度
- OCR 文本、区域和重试结果
- 自动攻击、寻路、换线及恢复状态
- 未捕获异常堆栈
- 故障截图保存路径

### 截图

发生自动化错误时，助手会先截取当前游戏画面并保存为 PNG。关键 OCR 流程也可以使用自己的标签保存最后一次失败画面，例如荒野传送官缺失或军团物资 OCR 失败。截图与对应模拟器日志位于同一共享目录，文件名包含模拟器标识、标签和时间戳。

## 当前边界

- 只支持横屏 `1280 × 720`、DPI `240` 的当前游戏布局。
- 不会根据其他分辨率自动缩放全部固定坐标。
- 只按中文界面文本训练和匹配 OCR。
- OCR 合并解决相邻碎片，不保证能修复严重模糊、遮挡或游戏尚未完成绘制的画面。
- 世界王未实现。
- 勇讨军团天将未实现，自动军务会跳过。
- 野境没有独立导航，暂时复用标记点流程。
- 固定坐标流程在游戏版本更新后必须重新实机验证。
- 不提供反检测、远程控制或账号安全保证。
